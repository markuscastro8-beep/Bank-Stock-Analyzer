# --- core data ---
pandas>=2.1
numpy>=1.26

# --- HTTP / parsing ---
requests>=2.31
urllib3>=2.0
beautifulsoup4>=4.12
lxml>=4.9
python-dotenv>=1.0
PyYAML>=6.0

# --- market data ---
yfinance>=0.2.40
sec-edgar-downloader>=5.0          # used optionally for bulk filing pulls

# --- charts ---
mplfinance>=0.12.10b0
matplotlib>=3.8
plotly>=5.20
# `lightweight-charts` (the Python wrapper) is intentionally NOT installed by
# default: it pulls in `pythonnet` which requires the .NET SDK to build on
# Windows. Our code only generates JSON payloads conforming to the
# TradingView Lightweight Charts wire format (consumable by the JS library
# directly). Install manually if you want the desktop renderer:
#   pip install lightweight-charts

# --- UI ---
rich>=13.7
streamlit>=1.32

# --- reports ---
openpyxl>=3.1
reportlab>=4.0

# --- testing ---
pytest>=8.0
